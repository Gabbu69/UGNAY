spring:
  datasource:
    url: jdbc:h2:mem:ugnay-test;MODE=MySQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1;NON_KEYWORDS=YEAR
    username: sa
    password:
  session:
    jdbc:
      initialize-schema: never
  jpa:
    hibernate:
      ddl-auto: validate
ugnay:
  security:
    bootstrap-admin-password: test-password
